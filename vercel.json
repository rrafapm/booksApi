{
    "version": 2,
    "builds": [
        {
            "src": "/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {   
            "src":"/api/books/(.*)",
            "dest": "/routes/bookRoutes.js"
        }
    ]
}